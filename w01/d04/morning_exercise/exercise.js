// identify all odd numbers between 1 to 100

// add all identified numbers together

// return the sum

function oddNum(){

  var odds;
  for (var x = 1; x < 100; x+=2 ){
    odds+=x;
  }
  return odds;
}

function reverseArr(arr){
  var revArr =[];
  for(var i=0, i<arr.length; i++){
    revArr.push(arr[1].split)
  }
}
